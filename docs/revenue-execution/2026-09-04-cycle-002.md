# Vector Praxis Revenue Execution — 2026-09-04 Cycle 002

## Objective

Repair Instagram / TikTok distribution without reopening the completed Bluesky route and without reviving the dead HTTP 410 legacy publisher.

## Current queue truth

- Bluesky item: `PUBLISHED`
- Instagram item: `READY / USER_APPROVED`
- TikTok item: `READY / USER_APPROVED`
- legacy publisher: HTTP 410 / dead

## Instagram direct route implemented

A direct official Instagram Reels provider was added:

- `scripts/social-provider-instagram.mjs`
- Reel container creation
- container status polling
- media publish
- permalink lookup
- fail-closed behavior when credentials are absent

Queue routing was updated so Instagram no longer depends on the legacy webhook.

Relevant commits:

- `b61fbd6a20f9e5c587855a6641ef2cc26d6ea6ac` — add Instagram direct publisher
- `e617817a36fccb629db0054eb3555515f3fa51ab` — route Instagram through direct provider
- `88aa697ba45bf5c70bb9d92b261d3b5a9596b8e4` — add Instagram preflight
- `3951eef3cfd3a7a7591c94e90dfc36a86bc4a96e` — wire GitHub Actions secrets
- `779dffcd7cb8ccf54585b4f2d88894ecf15dcd8f` — trigger live validation

## Live validation

GitHub Actions Social Publish Queue run:

`33823339650` — **success**

Observed provider state:

- Bluesky auth: healthy
- Instagram access token: missing
- Instagram user id: missing
- Instagram queue result: `READY_BUT_NOT_CONNECTED / instagram_direct`
- TikTok: still on dead legacy route / cooldown

This is the intended fail-closed state. No duplicate or unauthorized Instagram publish occurred.

## Provider research decision

Publer Public API is not selected as the default repair path because current public documentation limits API access to eligible paid/account tiers.

TikTok direct Content Posting API is technically available but requires a registered developer app, `video.publish` authorization, and an audit before unaudited clients can publish publicly. That is a higher-friction route for the current objective.

A Metricool ChatGPT plugin is available and advertises scheduling support for both Instagram and TikTok. This is now the preferred shared provider candidate because it can potentially close both remaining channels with one authenticated connection.

## Decision

- Bluesky direct: **KEEP / CLOSED**
- Instagram direct code: **KEEP / FALLBACK READY**
- dead legacy webhook: **STOP**
- Publer API as a new paid dependency: **HOLD / avoid unnecessary spend**
- TikTok developer-app integration: **HOLD** until the lower-friction Metricool route is tested
- Metricool plugin connection: **NEXT HUMAN GATE**

## Next completion condition

After Metricool is connected, test the already-approved Instagram and TikTok items, require external post IDs / public URLs, persist evidence, and only then mark each item `PUBLISHED`.
