# Vector Praxis Revenue Execution — 2026-09-03 Cycle 001

## Revenue truth

- Today's Purchase: **0**
- Today's Revenue: **0**
- Stripe mode checked: **live**
- Today's live PaymentIntents found before this QA cycle: **0**

The validation traffic below is QA traffic, not a customer conversion and not revenue.

## Funnel truth before the fix

Earlier PostHog QA events existed, but they were automation / validation traffic rather than customer demand. The previous Vector Hub origin also lacked a reliable Vector-specific Revenue OS funnel baseline.

After excluding QA / automation / bot traffic:

- Today's real Vector customer CTA clicks at the start of this cycle: **0 observed**
- Today's real Vector checkout clicks at the start of this cycle: **0 observed**
- Previous Vector Hub raw `$pageview` events for `vector-praxis-japan.user-ex26.chatgpt.site`: **0 observed in the queried 7-day window**

Traffic on `stratumpraxis.com` must not be misclassified as Vector Hub demand.

## Measurement fix

Commit: `1de4f6dc7471b71f0d5650448fef44740ac7fe71`

`app/layout.tsx` was changed so Vector queues:

- `traffic_session_start` once per browser session
- `funnel_view` on page load
- existing `data-event` click names
- `primary_cta_click` for the existing Cross-Agent Operating Kit commerce entry

Attribution properties include `asset_id`, `content_id`, `channel`, `route_id`, UTM fields, destination URL/path, and Cross-Agent product / offer IDs.

No product, price, payment provider, brand, or SNS was added.

## Build QA

GitHub Actions run `33702364933` for commit `1de4f6dc7471b71f0d5650448fef44740ac7fe71` completed successfully:

- `npm ci` — success
- `npm run build:vercel` — success
- `npm test` — success

## Vercel deployment evidence

The GitHub repository `stratumpraxis/vector-praxis-japan` is connected in Vercel Git settings.

Safe `main` pushes produced successful Vercel status checks for both currently connected projects:

- `Vercel – vector-praxis-japan` — **success**
- `Vercel – vector-praxis-japan-hub` — **success**

Production-path QA URL supplied and opened by the user:

`https://vector-praxis-japan-ibuoxtau0-stratumpraxis-7205s-projects.vercel.app/`

## Runtime analytics proof

PostHog runtime validation confirmed the production path is live.

### Page load

For the Vercel host above:

- `funnel_view` — **1 observed**

This proves the deployed Vector page loaded and its PostHog instrumentation executed.

### Paid CTA

The existing first paid resource is Cross-Agent Operating Kit (`$69+`) with route:

`route_id=vpj_hub_cross_agent_v1`

After the user opened the QA-tagged Vector URL and clicked the paid CTA once:

- `primary_cta_click` on the exact Vercel `$host` — **1 observed**
- the same event with `utm_source=codex_qa` — **1 observed**

This confirms the Vector production CTA and entry attribution work at runtime.

### Checkout

The user-provided screenshot visibly showed the Stripe card checkout form after the CTA flow. No purchase was requested or required.

PostHog also confirmed:

- `checkout_click` with `route_id=vpj_hub_cross_agent_v1` — **1 observed**
- its downstream `utm_source` — **vector_praxis**

The source transition is expected because the Cross-Agent destination URL intentionally identifies Vector Praxis as the downstream acquisition source. The original QA source remains measurable on the upstream `primary_cta_click`, while `route_id` connects the downstream checkout event to the same Vector route.

## Closure decision

The original Vector Hub measurement/deployment blocker is **CLOSED**:

- Git connection — **complete**
- GitHub → Vercel auto-deploy — **confirmed**
- public browser load — **confirmed**
- `funnel_view` delivery — **confirmed**
- paid CTA runtime event — **confirmed**
- CTA → Stripe Checkout navigation — **confirmed visually**
- downstream `checkout_click` route measurement — **confirmed**

QA traffic must remain excluded from real customer performance metrics.

## Vercel architecture decision

Use one canonical Vercel project:

- `vector-praxis-japan` = **KEEP / PRODUCTION**
- `vector-praxis-japan-hub` = **STOP / REMOVE**

Autonomous QA should use Vercel Preview Deployments from pull requests / non-production branches inside the canonical project instead of maintaining a second production surface.

## Revenue decision

- Measurement fix: **KEEP**
- Existing $69 Cross-Agent route: **KEEP**
- CTA/offer rewrite before real baseline: **HOLD**
- New product/content build before demand evidence: **HOLD**

## Next highest-probability action

The technical funnel is no longer the bottleneck. The next bottleneck is **real qualified traffic → paid CTA → Purchase**.

Next actions:

1. Retire the redundant `vector-praxis-japan-hub` Vercel project so future `main` pushes do not create duplicate deployments.
2. Keep QA traffic excluded from customer metrics.
3. Send measured Japanese demand through the already-published Zenn / existing distribution assets into this validated route.
4. Observe the first real `primary_cta_click`, then `checkout_click`, then Purchase.
5. Change only the largest measured bottleneck after real traffic exists.

No new revenue asset is required to prove this route; the next objective is the first real customer Purchase.
