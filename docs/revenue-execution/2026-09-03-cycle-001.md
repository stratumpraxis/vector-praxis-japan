# Vector Praxis Revenue Execution — 2026-09-03 Cycle 001

## Revenue truth

- Today's Purchase: **0**
- Today's Revenue: **0**
- Stripe mode checked: **live**
- Today's live PaymentIntents found: **0**

## Funnel truth

PostHog initially showed 2 `primary_cta_click` and 2 `checkout_click` events today, but inspection showed both were `codex_qa` / `automation` traffic. They are **not customer signals**.

After excluding QA / automation / bot traffic:

- Today's real Vector customer CTA clicks: **0 observed**
- Today's real Vector checkout clicks: **0 observed**
- Previous Vector Hub raw `$pageview` events for `vector-praxis-japan.user-ex26.chatgpt.site`: **0 observed in the queried 7-day window**

The current PostHog project also contains recent real traffic on `stratumpraxis.com`; that traffic must not be misclassified as Vector Hub traffic.

## Measurement fix

Commit: `1de4f6dc7471b71f0d5650448fef44740ac7fe71`

Changed `app/layout.tsx` so Vector queues these PostHog events:

- `traffic_session_start` once per browser session
- `funnel_view` on page load
- existing `data-event` click names
- `primary_cta_click` for the existing Cross-Agent Operating Kit commerce entry

Attribution properties include:

- `asset_id`
- `content_id`
- `channel`
- `route_id`
- `utm_source`
- `utm_medium`
- `utm_campaign`
- `utm_content`
- destination URL/path on clicks
- Cross-Agent product / offer IDs for the primary commerce CTA

No product, price, payment provider, brand, or SNS was added.

## QA evidence

GitHub Actions run `33702364933` for commit `1de4f6dc7471b71f0d5650448fef44740ac7fe71` completed successfully.

Verified steps:

- `npm ci` — success
- `npm run build:vercel` — success
- `npm test` — success

## Vercel deployment evidence

The GitHub repository `stratumpraxis/vector-praxis-japan` is connected in Vercel Git settings.

Safe `main` pushes produced successful Vercel status checks. Latest verified commit:

`8fe73943d0675f8f44185e1ee406b73c4a14064a`

Successful status contexts:

- `Vercel – vector-praxis-japan` — **success**
- `Vercel – vector-praxis-japan-hub` — **success**

This proves GitHub → Vercel auto-deploy is functioning.

User-supplied deployment URL used for production-path verification:

`https://vector-praxis-japan-ibuoxtau0-stratumpraxis-7205s-projects.vercel.app/`

## Live analytics proof

PostHog was queried after the deployment URL was opened.

`funnel_view` breakdown by `$host` for the current validation window showed:

- `vector-praxis-japan-ibuoxtau0-stratumpraxis-7205s-projects.vercel.app` — **1 observed funnel_view**

This is runtime evidence that the Vercel page loaded in a browser and the deployed Vector PostHog instrumentation executed successfully.

The source also confirms that the first paid resource is the existing Cross-Agent Operating Kit (`$69+`) and its destination is the tracked Stratum product route with `route_id=vpj_hub_cross_agent_v1`.

At the time of this evidence update, no `primary_cta_click` carrying this Vercel `$host` had yet been observed. Therefore page-load analytics is closed; paid CTA runtime click verification remains the smallest open check.

## Vercel architecture decision

Use **one canonical Vercel project only**:

- `vector-praxis-japan` = production / Revenue measurement baseline / canonical Vercel project
- `vector-praxis-japan-hub` = redundant duplicate and deprecation target

Reason: both projects currently deploy the same `main` commit, causing duplicate builds, duplicate deployment surfaces, possible analytics host fragmentation, and future ambiguity about the authoritative production URL.

QA / autonomous improvement work does **not** require a second Vercel project. Use Vercel Preview Deployments generated from pull requests / non-production branches inside the single `vector-praxis-japan` project, then merge to `main` only after QA.

## Current blocker

The original measurement/deployment blocker is substantially closed:

- Git connection — **complete**
- GitHub → Vercel auto-deploy — **confirmed**
- Public browser load — **confirmed indirectly by PostHog host event**
- `funnel_view` delivery — **confirmed**
- Cross-Agent paid CTA source route — **confirmed in source**
- Cross-Agent paid CTA runtime click event — **not yet observed on the Vercel host**

## Decision

- Measurement fix: **KEEP**
- `vector-praxis-japan`: **KEEP / PRODUCTION**
- `vector-praxis-japan-hub`: **STOP / REMOVE after production CTA verification**
- QA strategy: **USE VERCEL PREVIEW DEPLOYMENTS, NOT A SECOND PROJECT**
- CTA/offer rewrite before a measurable Vector baseline: **HOLD**
- New content/product creation: **HOLD**

## Next highest-probability action

1. Open the production Vercel deployment with QA attribution.
2. Click the existing Cross-Agent Operating Kit paid CTA once.
3. Verify `primary_cta_click` arrives with the Vercel `$host` and `cta_id=vector_hub_cross_agent_kit`.
4. Keep this QA traffic excluded from customer metrics.
5. After CTA runtime proof, remove/disconnect redundant `vector-praxis-japan-hub`.
6. Collect a real Vector traffic baseline before changing any conversion variable.

No new asset should be built before this measurement loop is live.
