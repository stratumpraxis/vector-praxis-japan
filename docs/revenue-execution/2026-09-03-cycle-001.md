# Vector Praxis Revenue Execution — 2026-09-03 Cycle 001

## Revenue truth

- Today's Purchase: **0**
- Today's Revenue: **0**
- Stripe mode checked: **live**
- Today's live PaymentIntents found before this QA cycle: **0**

The validation traffic below is QA traffic, not a customer conversion and not revenue.

## Funnel truth before the fix

Earlier PostHog QA events existed, but they were automation / validation traffic rather than customer demand. The previous Vector Hub origin also lacked a reliable Vector-specific Revenue OS funnel baseline.

After excluding QA / automation / bot traffic:

- Today's real Vector customer CTA clicks at the start of this cycle: **0 observed**
- Today's real Vector checkout clicks at the start of this cycle: **0 observed**
- Previous Vector Hub raw `$pageview` events for `vector-praxis-japan.user-ex26.chatgpt.site`: **0 observed in the queried 7-day window**

Traffic on `stratumpraxis.com` must not be misclassified as Vector Hub demand.

## Measurement fix

Initial instrumentation commit:

`1de4f6dc7471b71f0d5650448fef44740ac7fe71`

`app/layout.tsx` was changed so Vector queues:

- `traffic_session_start` once per browser session
- `funnel_view` on page load
- existing `data-event` click names
- `primary_cta_click` for the existing Cross-Agent Operating Kit commerce entry

Attribution properties include `asset_id`, `content_id`, `channel`, `route_id`, UTM fields, destination URL/path, and Cross-Agent product / offer IDs.

A live QA click then exposed an attribution defect: the paid CTA inherited page-level `route_id=vp_main` and did not persist `destination_host`.

Attribution correction commit:

`a0da881c510b9f8d58c4d124442ad7f4a86e5e55`

After the correction, page-level events keep the page route while paid CTA events derive the downstream route and destination from the actual link URL.

No product, price, payment provider, brand, or SNS was added.

## Build QA

GitHub Actions run `33702364933` for the initial instrumentation completed successfully:

- `npm ci` — success
- `npm run build:vercel` — success
- `npm test` — success

The attribution correction commit also received successful Vercel status checks on the connected projects.

## Vercel deployment evidence

The GitHub repository `stratumpraxis/vector-praxis-japan` is connected in Vercel Git settings.

Safe `main` pushes produced successful Vercel status checks for both currently connected projects:

- `Vercel – vector-praxis-japan` — **success**
- `Vercel – vector-praxis-japan-hub` — **success**

Production-path QA URL supplied and opened by the user:

`https://vector-praxis-japan-ibuoxtau0-stratumpraxis-7205s-projects.vercel.app/`

## Runtime analytics proof

PostHog runtime validation confirmed the production path is live.

### Page load

For the Vercel host above:

- `funnel_view` — **observed**

This proves the deployed Vector page loaded and its PostHog instrumentation executed.

### Paid CTA

The existing first paid resource is Cross-Agent Operating Kit (`$69+`) with route:

`route_id=vpj_hub_cross_agent_v1`

After the post-fix production click:

- `primary_cta_click`
- `route_id=vpj_hub_cross_agent_v1`
- `destination_host=stratumpraxis.com`
- `cta_id=vector_hub_cross_agent_kit`

were observed together.

The older QA event with `route_id=vp_main` and no destination host is pre-fix history and must not be used as the post-fix baseline.

### Downstream checkout direction

The user-provided screenshot visibly showed the Cross-Agent product page after the Vector CTA flow. No purchase was requested or required.

PostHog separately confirmed downstream paid-intent activity on the same route toward Stripe:

- `route_id=vpj_hub_cross_agent_v1`
- `destination_host=buy.stripe.com`
- `cta_id=cross_agent_personal_product_hero`

This confirms that Vector → Stratum product → Stripe direction is observable at click level. It does **not** prove a purchase.

## Zenn demand baseline

The published Zenn article route is:

`route_id=zenn_ai_agent_bottleneck_v1`

For the queried 2026-09-01 through 2026-09-03 window, PostHog observed:

- `funnel_view` — **0**
- `primary_cta_click` — **0**
- `checkout_click` — **0**

Recent `funnel_view` source values did not include `zenn`, and recent `$pageview` referring domains did not include `zenn.dev`.

Therefore the current measured Zenn bottleneck is **distribution / real traffic**, not CTA instrumentation, product navigation, or checkout direction.

## Closure decision

The original Vector Hub measurement/deployment blocker is **CLOSED**:

- Git connection — **complete**
- GitHub → Vercel auto-deploy — **confirmed**
- public browser load — **confirmed**
- `funnel_view` delivery — **confirmed**
- post-fix paid CTA attribution — **confirmed**
- Vector → Stratum product navigation — **confirmed visually**
- Stratum product → Stripe direction event — **confirmed in analytics**

QA traffic must remain excluded from real customer performance metrics.

## Vercel architecture decision

Use one canonical Vercel project:

- `vector-praxis-japan` = **KEEP / PRODUCTION**
- `vector-praxis-japan-hub` = **STOP / REMOVE**

Autonomous QA should use Vercel Preview Deployments from pull requests / non-production branches inside the canonical project instead of maintaining a second production surface.

## Revenue decision

- Measurement fix: **KEEP**
- Existing $69 Cross-Agent route: **KEEP**
- CTA/offer rewrite before real baseline: **HOLD**
- New paid product before demand evidence: **HOLD**
- New Zenn article before the existing article gets measurable distribution: **HOLD**

## Social distribution repair

The old queue sent every platform through `SOCIAL_PUBLISH_WEBHOOK_URL`. The endpoint now returns HTTP 410, so one dead publisher could block the full queue.

Direct Bluesky routing was added without new paid infrastructure:

- `scripts/social-provider-bluesky.mjs` — direct official AT Protocol session, video processing, `app.bsky.embed.video`, link facet, and post creation
- `scripts/social-queue.mjs` — Bluesky direct-provider routing; legacy cooldown no longer blocks Bluesky
- `scripts/social-provider-preflight.mjs` — Bluesky credential presence/auth preflight without storing tokens
- `.github/workflows/social-publish.yml` — Bluesky env wiring and fail-closed execution
- existing approved Bluesky item requeued without changing its approved copy/media

Relevant implementation commits:

- `1b0723176c525955c6f71058b63f1080132ab8f9`
- `2aef9eea56e156c270b51981a58336d8f5f15de8`
- `37b6d20e9269e18904ac72a02b607d0abedfb890`
- `6aa5c4c7387fd914c3d721532491187938e3a38b`
- `17bda53cb82a9550fc5f06d05dc504df6cdc35a2`

Validation trigger commit:

`173ab4310fa6eef76df11232451a5633fe55bce2`

GitHub Actions run:

`33721303221` — **success**

The run reached and completed both `Preflight direct social providers` and `Publish approved due social items` successfully.

Observed provider state after the run:

- Bluesky handle — **present**
- `BLUESKY_APP_PASSWORD` — **missing**
- Bluesky queue item — `READY_BUT_NOT_CONNECTED`
- Instagram — legacy publisher cooldown / HTTP 410
- TikTok — legacy publisher cooldown / HTTP 410

This is the intended fail-closed state: no unauthorized publish occurred and the approved Bluesky item stayed READY.

Recurring workflow efficiency was also improved so ffmpeg/Pillow installation happens only when the campaign video is actually missing:

`69b73c363a28bd47f268d41a7d3da993074bee4d`

## Current highest-probability action

The technical purchase route and the new free Bluesky write path are ready. The remaining Bluesky Human Gate is now exactly one secret:

`BLUESKY_APP_PASSWORD`

After that secret is registered in GitHub Actions, the next scheduled or manually triggered Social Publish Queue run can authenticate, upload the already-approved MP4, create the post, store `external_post_id` / public URL, and close the Bluesky publish loop automatically.

Instagram/TikTok remain separate blockers and must not hold up Bluesky.

No new paid revenue asset is required; the next objective remains **real qualified traffic → paid CTA → Checkout → first Purchase**.
