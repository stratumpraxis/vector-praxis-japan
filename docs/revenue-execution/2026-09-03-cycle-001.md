# Vector Praxis Revenue Execution — 2026-09-03 Cycle 001

## Revenue truth

- Today's Purchase: **0**
- Today's Revenue: **0**
- Stripe mode checked: **live**
- Today's live PaymentIntents found: **0**

## Funnel truth

PostHog initially showed 2 `primary_cta_click` and 2 `checkout_click` events today, but inspection showed both were `codex_qa` / `automation` traffic. They are **not customer signals**.

After excluding QA / automation / bot traffic:

- Today's real Vector customer CTA clicks: **0 observed**
- Today's real Vector checkout clicks: **0 observed**
- Recent Vector Hub raw `$pageview` events for `vector-praxis-japan.user-ex26.chatgpt.site`: **0 observed in the queried 7-day window**

The current PostHog project does contain recent real traffic on `stratumpraxis.com`, but that must not be misclassified as Vector Hub traffic.

## Biggest bottleneck

**Measurement gap at the Vector Hub origin.**

The Vector source already loaded PostHog, but it did not emit the Revenue OS custom funnel events used by the operating model. That prevented reliable Vector-specific `traffic_session_start → primary_cta_click → checkout_click` decisions.

## One fix executed

Commit: `1de4f6dc7471b71f0d5650448fef44740ac7fe71`

Changed `app/layout.tsx` so Vector now queues these PostHog events:

- `traffic_session_start` once per browser session
- `funnel_view` on page load
- existing `data-event` click names
- `primary_cta_click` for the existing Cross-Agent Operating Kit commerce entry

Attribution properties include:

- `asset_id`
- `content_id`
- `channel`
- `route_id`
- `utm_source`
- `utm_medium`
- `utm_campaign`
- `utm_content`
- destination URL/path on clicks
- Cross-Agent product / offer IDs for the primary commerce CTA

No product, price, payment provider, brand, SNS, or domain was added.

## QA evidence

GitHub Actions run `33702364933` for commit `1de4f6dc7471b71f0d5650448fef44740ac7fe71` completed successfully.

Verified steps:

- `npm ci` — success
- `npm run build:vercel` — success
- `npm test` — success

## Public deployment status

Canonical production URL recorded by the repository:

`https://vector-praxis-japan.user-ex26.chatgpt.site/`

The GitHub repository `stratumpraxis/vector-praxis-japan` is visibly connected in Vercel Git settings. A safe `main` push at commit `816873080b583e809eb0eeaed0aa68acc30d2cea` produced successful Vercel checks for both `vector-praxis-japan` and `vector-praxis-japan-hub`.

This proves GitHub → Vercel auto-deploy is functioning.

## Vercel architecture decision

Use **one canonical Vercel project only**:

- `vector-praxis-japan` = production / Revenue measurement baseline / canonical Vercel project
- `vector-praxis-japan-hub` = redundant duplicate and deprecation target

Reason: both projects currently deploy the same `main` commit, causing duplicate builds, duplicate deployment surfaces, possible analytics host fragmentation, and future ambiguity about the authoritative production URL.

QA / autonomous improvement work does **not** require a second Vercel project. Use Vercel Preview Deployments generated from pull requests / non-production branches inside the single `vector-praxis-japan` project, then merge to `main` only after QA. This preserves autonomous iteration without creating a second production surface.

## Required human gate / blocker

Git connection is complete. GitHub → Vercel auto-deploy is confirmed.

The remaining cleanup action is to remove or disconnect the redundant `vector-praxis-japan-hub` Vercel project. The currently connected Vercel tool surface does not expose a project-delete action, so this cleanup cannot be asserted as completed from automation.

## Decision

- Measurement fix: **KEEP**
- `vector-praxis-japan`: **KEEP / PRODUCTION**
- `vector-praxis-japan-hub`: **STOP / REMOVE**
- QA strategy: **USE VERCEL PREVIEW DEPLOYMENTS, NOT A SECOND PROJECT**
- CTA/offer rewrite before a measurable Vector baseline: **HOLD**
- New content/product creation: **HOLD**

## Next highest-probability action

1. Keep `vector-praxis-japan` as the single Vercel production project.
2. Remove or disconnect `vector-praxis-japan-hub` to stop duplicate auto-deploys.
3. Open the production Vercel Public URL using a QA-tagged URL (`utm_source=codex_qa&utm_medium=automation`).
4. Verify `traffic_session_start` and `funnel_view` arrive with the Vector host.
5. Test the existing Cross-Agent Kit CTA and verify `primary_cta_click` attribution.
6. Exclude that QA traffic from customer metrics.
7. Collect a real Vector traffic baseline before changing any conversion variable.

No new asset should be built before this measurement loop is live.
